# Ambient Audio

Place `space_ambient.ogg` here. AudioManager loops it automatically via the `finished` signal.

Recommended format: stereo `.ogg` Vorbis, 44 100 Hz, seamlessly loopable, –18 LUFS.
Target duration: ≥ 2 minutes before the loop point to avoid noticeable repetition.
